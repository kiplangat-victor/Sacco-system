---
apiVersion: v1
kind: Namespace
metadata:
  name: production
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounts
  namespace: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: accounts
  template:
    metadata:
      labels:
        app: accounts
    spec:
      containers:
        - name: pod-info-container
          image: firsttestcontainer.azurecr.io/accounts:241
          ports:
            - containerPort: 9006
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
      imagePullSecrets:
        - name: acr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: accounts-service
  namespace: production
spec:
  selector:
    app: accounts
  ports:
    - port: 80
      targetPort: 9006
  type: LoadBalancer
